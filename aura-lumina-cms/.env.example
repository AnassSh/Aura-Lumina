# Database – PostgreSQL (required for production)
# Get a free Postgres DB from: Neon, Supabase, or Vercel Postgres
# Example format:
# DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

DATABASE_URL=

# Used to encrypt JWT tokens
PAYLOAD_SECRET=

# Used to configure CORS, format links, etc. No trailing slash
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Optional: for cron jobs
CRON_SECRET=

# Optional: for preview
PREVIEW_SECRET=

# Vercel Blob – required for media uploads on Vercel (read-only filesystem)
# Create at: Vercel Dashboard → Storage → Blob → Create Store
# BLOB_READ_WRITE_TOKEN=
