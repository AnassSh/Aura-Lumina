# -----------------------------------------------------------------------------
# Aura Lumina – optional env for CRM + automation
# Copy to .env.local and set values as needed.
# -----------------------------------------------------------------------------

# n8n webhook URLs (e.g. self-hosted n8n) – when set, POST /api/contact
# forwards payloads so you can trigger Telegram (or other) notifications and
# send data to Payload CMS or any CRM.
# Example if n8n runs at http://localhost:5678 or your server:
# N8N_WEBHOOK_URL_ORDER=http://localhost:5678/webhook/order
# N8N_WEBHOOK_URL_PARTNER=http://localhost:5678/webhook/partner
# N8N_WEBHOOK_URL_GENERAL=http://localhost:5678/webhook/general

# N8N_WEBHOOK_URL_ORDER=
# N8N_WEBHOOK_URL_PARTNER=
# N8N_WEBHOOK_URL_GENERAL=

# -----------------------------------------------------------------------------
# Payload CMS (headless CMS for products, clients, partners)
# Set when you connect Aura Lumina to your Payload instance.
# -----------------------------------------------------------------------------
# PAYLOAD_API_URL=http://localhost:3001/api
# PAYLOAD_API_KEY=
